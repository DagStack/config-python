"""Single-source version for dagstack-config.

Hatch reads `__version__` from here (see pyproject.toml `[tool.hatch.version]`).
The `vX.Y.Z` tag must match this value — the CI publish workflow validates it.
"""

__version__ = "0.5.0"
